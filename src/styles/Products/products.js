import styled from "styled-components";

const StyledProducts = styled.section`
  .links_wrapper {
    margin-left: auto;
  }
  .card__wrapper {
    margin-bottom: 20px;
    .ant-card-body {
      padding-top: 0;
    }
  }
`;

export { StyledProducts };
